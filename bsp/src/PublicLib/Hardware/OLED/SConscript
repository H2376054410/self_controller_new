from building import *

cwd = GetCurrentDir()

src = Glob('*.c')

CPPPATH = [cwd]

if GetDepend(['BSP_USING_OLED']):
    group = DefineGroup('Public/Hardware/Oled', src, depend = [''], CPPPATH = CPPPATH)
else:
    group = []

Return('group')
