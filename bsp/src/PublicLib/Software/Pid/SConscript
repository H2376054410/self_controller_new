from building import *

cwd = GetCurrentDir()

src = Glob('*.c')

CPPPATH = [cwd]

if GetDepend(['BSP_USING_PID']):
    group = DefineGroup('Public/Software/Pid', src, depend = [''], CPPPATH = CPPPATH)
else:
    group = []

Return('group')
