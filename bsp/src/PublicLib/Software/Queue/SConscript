from building import *

cwd = GetCurrentDir()

src = Glob('*.c')

CPPPATH = [cwd]

if GetDepend(['BSP_USING_QUEUE']):
    group = DefineGroup('Public/Software/Queue', src, depend = [''], CPPPATH = CPPPATH)
else:
    group = []

Return('group')
