from building import *

cwd = GetCurrentDir()

src = Glob('*.c')

CPPPATH = [cwd]

if GetDepend(['BSP_USING_DATASERVER']):
    group = DefineGroup('Public/Software/DataServer', src, depend = [''], CPPPATH = CPPPATH)
else:
    group = []

Return('group')
