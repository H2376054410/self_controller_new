from building import *

cwd = GetCurrentDir()

src = Glob('*.c')

CPPPATH = [cwd]

if GetDepend(['BSP_USING_SETPLANNING']):
    group = DefineGroup('Public/Software/SetPlanning', src, depend = [''], CPPPATH = CPPPATH)
else:
    group = []

Return('group')
