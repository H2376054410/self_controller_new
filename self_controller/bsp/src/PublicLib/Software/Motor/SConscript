from building import *

cwd = GetCurrentDir()

src = Glob('*.c')

CPPPATH = [cwd]

if GetDepend(['BSP_USING_MOTORLIB']):
    group = DefineGroup('Public/Software/Motor', src, depend = [''], CPPPATH = CPPPATH)
else:
    group = []

Return('group')
